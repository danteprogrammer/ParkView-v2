<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ParkView" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.ParkView" parent="Base.Theme.ParkView" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@drawable/header_background_image</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>

        <item name="postSplashScreenTheme">@style/Theme.ParkView</item>
    </style>
</resources>